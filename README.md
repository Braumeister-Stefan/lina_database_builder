# lina_database_builder
This tool attempts to find publically available tablet data (with date point estimate where available) containing linear a minoan writing, provide data cleaning services, descriptive summary statistics and clustering of data. This model saves the result as a csv database
